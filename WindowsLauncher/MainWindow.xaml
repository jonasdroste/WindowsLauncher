<Window x:Class="WindowsLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsLauncher"
        xmlns:vm="clr-namespace:WindowsLauncher.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        Title="MainWindow" 
        Height="450" 
        Width="800"
        Background="DarkGray" 
        >
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding ApplicationName}" d:Text="ApplicationName"  MinWidth="100"></TextBlock>
            <Button Command="{Binding ChangeApplicationNameCommand}">Change Application Name</Button>
        </StackPanel>
        
    </Grid>
</Window>
